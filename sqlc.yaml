version: "2"
sql:
  - engine: "postgresql"
    queries: "api/query.sql"
    schema: "migrations"
    gen:
      go:
        package: "models"
        out: "internal/models"
        sql_package: "pgx/v5"
